worker_processes auto;

events {
    worker_connections 1024;
}

http {
    upstream backend {
        server api01:9999;
        server api02:9999;
    }

    server {
        listen 9999;

        location / {
            proxy_pass http://backend;
        }
    }
}

